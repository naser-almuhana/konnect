import { UserData } from "@/types/db.types"

interface EditProfileButtonProps {
  user: UserData
}

export function EditProfileButton({}: EditProfileButtonProps) {
  return <div>EditProfileButton</div>
}
